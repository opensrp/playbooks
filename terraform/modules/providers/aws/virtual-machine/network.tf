# Security Groups

# module "domain-name" {
#   source           = "../domain-name"
#   dns_zone_name    = "${var.vm_dns_zone_name}"
#   dns_names        = "${var.vm_domain_names}"
#   dns_ip_addresses = ["${aws_instance.main.public_ip}"]
# }
